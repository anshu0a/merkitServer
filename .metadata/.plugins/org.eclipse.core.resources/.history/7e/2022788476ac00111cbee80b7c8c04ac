package com.merkit.service;

import org.springframework.security.core.Authentication;

import com.merkit.dto.req.AIRequest;
import com.merkit.dto.res.AIChatResponse;

public interface AIChatService {
	
	AIChatResponse getChat(Authentication auth, String chatId);
	AIChatResponse addNewChat(AIRequest req, Authentication auth);
	
	

}
