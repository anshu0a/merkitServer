package com.merkit.service;

import org.springframework.security.core.Authentication;

import com.merkit.dto.req.AIRequest;
import com.merkit.entity.AIChat;

public interface AIChatService {
	
//	String getAnswer(String prompt);
	AIChat getChat(Authentication auth, Long chatId);
	AIChat addNewChat(AIRequest req, Authentication auth);
	AIChat addOldChat(AIRequest req, Authentication auth,Long chatId);
	
	

}
